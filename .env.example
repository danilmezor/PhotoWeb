VITE_SITE_URL=https://your-domain.com

# MailerLite newsletter (public embedded-form IDs — safe to expose client-side).
# Create an embedded form in MailerLite, then copy the account ID and form ID
# from its endpoint: https://assets.mailerlite.com/jsonp/<ACCOUNT_ID>/forms/<FORM_ID>/subscribe
VITE_MAILERLITE_ACCOUNT_ID=
VITE_MAILERLITE_FORM_ID=
